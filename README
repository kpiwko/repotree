Utility to recursively install artifacts into a Maven repository
Based on Aether 1.7

usage:

mvn package

java -jar target/repotree-jar-with-dependencies <path-to-repository> <directory-to-mass-install> 
